<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deposit Pokemon</title>
</head>
<body>
    <img src="/PokemonLogo.png" alt="Pokemon Logo" width="341" height="125"><br>
    <form id="depositPokemon" action="/Deposit" method="post" onsubmit="confirmSubmit()">
        <fieldset>
            <strong>Deposit a pokemon: </strong> <input type="text" name="pokeName" id="pokeName">
        </fieldset>
        <button type = "submit">Deposit</button>
        <br>
        <a href="/">HOME</a>
    </form>
    <script>
        function confirmSubmit() {
            const pokemonName = document.getElementById("pokeName").value;
            return confirm(`Are you sure you want to deposit ${pokemonName}?`);
        }
    </script>
    <% if (errorMessage) { %>
        <script>
            alert("<%= errorMessage %>");
        </script>
    <% } %>
</body>
</html>