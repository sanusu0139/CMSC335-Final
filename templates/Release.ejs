<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deposit Pokemon</title>
</head>
<body>
    <img src="/PokemonLogo.png" alt="Pokemon Logo" width="341" height="125"><br>
    <h1>Release a Pokemon</h1> 
    <form id="releasePokemon" action="/Release" method="post" onsubmit="confirmSubmit()">
        <fieldset>
            <%- availablePokemon %>
            <br>
            <strong>Release a pokemon: </strong> <input type="text" name="pokeName" id="pokeName">
        </fieldset>
        <button type = "submit">Release</button>
        <br>
        <a href="/">HOME</a>
    </form>
    <script>
        function confirmSubmit() {
            const pokemonName = document.getElementById("pokeName").value;
            return confirm(`Are you sure you want to release ${pokemonName}?`);
        }
    </script>
    <% if (errorMessage) { %>
        <script>
            alert("<%= errorMessage %>");
        </script>
    <% } %>
</body>
</html>